services:
  tav:
    image: textannoviz:dev
  proxy:
    image: nginx:stable-alpine-slim
    ports:
    - "5183:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf
      - ./config.json:/etc/nginx/config.json
    depends_on:
      - tav